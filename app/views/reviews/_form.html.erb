<%= form_for @review do |f| %>

  <%= f.label :rating, "Rating: " %>
  <%= f.number_field(:rating, in: 1..5, step: 1) %>

  <%= f.label :comment, "Leave a review:" %>
  <%= f.text_area :comment %>

  <%= f.submit %>

  <% end %>
