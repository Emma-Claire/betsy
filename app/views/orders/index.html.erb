to be manipulated for orders show.html.erb

<article>
  <table>
    <thead>
      <th>Product</th>
      <th>Image</th>
      <th>Quantity</th>
      <th>Unit Price</th>
    </thead>
    <tbody>
      <% @order.orderedproduct.each do |op| %>
      <tr>
        <td><%= Product.find_by(id: op.product_id).name %></td>
        <td><%= image_tag Product.find_by(id: op.product_id).photo_url %></td>
        <td><%= op.quantity %></td>
        <td>$<%= op.product.price %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</article>
