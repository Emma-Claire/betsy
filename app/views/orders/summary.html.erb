<h2> Order Summary </h2>

<%= render partial: "ops_table", locals: { headers: true, ops: @order.orderedproducts } %>

<%= render partial: "billing_info", locals: { order: @order } %>

<p> Feeling Buyer's Remorse? </p><%= link_to "Cancel Order", cancel_order_path, method: :patch, data: {confirm: "Are you sure you want to cancel this item?"}, class: "button alert" %>
