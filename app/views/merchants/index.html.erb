<h3> Plantsy Shops</h3>

<section class="row">
  <% @merchants.each do |merchant| %>
    <div class="large-4 columns">
      <ul>
        <li class="shop"> <%= link_to merchant.username, merchant_path(merchant.id) %>'s Plantsy Shop</li>
      </ul>
    </div>
    <% end %>
</div>
